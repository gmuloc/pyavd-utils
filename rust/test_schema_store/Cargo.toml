[package]
name = "test_schema_store"
edition = "2024"

[dependencies]
reqwest = { version = "0.12.24", default-features = false, features = [
    "blocking",
    "default-tls",
] }

[lib]
crate-type = ["cdylib", "lib"]
